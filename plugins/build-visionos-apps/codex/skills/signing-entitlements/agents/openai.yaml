interface:
  display_name: "Signing & Entitlements"
  short_description: "Inspect signing, capabilities, and privacy configuration for visionOS apps"
  default_prompt: "Use $signing-entitlements to classify simulator vs device/distribution signing failures, verify entitlements/capabilities/privacy keys, and provide the minimum fix path with explicit guidance on when to switch to $build-run-debug."
